#!/usr/bin/env ruby

# frozen_string_literal: true

lib_path = File.join(File.expand_path(__dir__), '..', 'lib')

$LOAD_PATH.unshift(lib_path) unless $LOAD_PATH.include?(lib_path)

require 'ex18'

# Ex18.sprites!(logger: false) it works too

Ex18.sprites!
