#!/usr/bin/env ruby

# frozen_string_literal: true

require 'pg'

conn = PG.connect(host: 'db', dbname: 'seven_dbs', user: 'postgres', password: 'postgres')

pp conn.exec("DELETE FROM countries WHERE country_code = 'll'")
