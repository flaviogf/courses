struct date {
  int month;
  int day;
  int year;
};

void day_of_the_week(struct date, char *);

static int index_day_of_the_week(struct date);

static int n(struct date);

static int f(int, int);

static int g(int);
