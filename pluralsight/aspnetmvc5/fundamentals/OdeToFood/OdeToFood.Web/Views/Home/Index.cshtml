@model IEnumerable<OdeToFood.Data.Models.Restaurant>

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>ASP.NET</h1>
    <p>There are @Model.Count() restaurants in the database.</p>
</div>

<ul>
    @foreach(var it in Model)
    {
        <li>@it.Name</li>
    }
</ul>
