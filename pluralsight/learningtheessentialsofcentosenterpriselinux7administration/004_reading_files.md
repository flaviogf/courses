# Reading Files

```sh
cat /etc/hosts

cat /etc/hostname

cat /etc/host /etc/hostname

wc -l /etc/services

less /etc/services

!$ # the last argument

head /etc/services

head -n 3 /etc/services

tail -n 3 /etc/services

tail /etc/passwd
```
